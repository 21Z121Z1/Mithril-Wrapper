- [x] `normalize_vulkan_incompatible_layouts()` 把 UBO 块 `layout(packed)` 改写为 `layout(std140)` — 代码 L744+L766 确认；relaxed 测试 Case 3 通过
- [x] `normalize_vulkan_incompatible_layouts()` 把 UBO 块 `layout(shared)` 改写为 `layout(std140)` — 代码 L744 (`packed || shared`) + L766 (`uniform`→std140) 确认，与 packed 同机制
- [x] `normalize_vulkan_incompatible_layouts()` 把 SSBO 块 `layout(packed/shared)` 改写为 `layout(std430)` — 代码 L766 (`buffer`→std430) 确认
- [x] `normalize_vulkan_incompatible_layouts()` 幂等（`std140`/`std430` 不被重复改写） — relaxed 测试 Case 5 通过
- [x] `normalize_vulkan_incompatible_layouts()` 跳过 `//` 与 `/* */` 注释内的出现 — 代码 L758 `is_in_comment` 确认
- [x] `normalize_gl_legacy_constructs()` 把 `gl_FragColor` 改写为合成命名输出 + 引用改写 — relaxed 测试 Case 4 通过
- [x] `normalize_gl_legacy_constructs()` 不影响已显式声明输出的着色器 — 代码 L829 fast-path（无 `gl_FragColor` 即 no-op）确认
- [x] `glsl_to_spirv()` 在预处理阶段为所有编译尝试调用两个归一化 pass — 调用于 L988/L989，`source_unwrapped` 备份于 L1009（归一化之后）使 fallback 继承
- [x] `shader3`（第三级 relaxed）块已从 `glsl_to_spirv()` 移除 — grep `shader3` 0 匹配
- [x] 不再调用 `setEnvInputVulkanRulesRelaxed()` — grep 0 匹配
- [x] `Shader.cpp` 内无 `EShClientVulkan` 残留（grep 干净） — grep 0 匹配
- [x] 两级 strict 回退（wrapped → unwrapped，均为 `EShClientOpenGL`）保留 — grep `setEnvInput` 仅 2 处，均 `EShClientOpenGL`
- [x] `verify/shader_relaxed_fallback_test.cpp` 已更新为验证归一化 pass 行为 — 7 用例，内联镜像两个归一化 pass
- [x] 真实 Minecraft vertex/fragment 着色器的无回归检查保留 — relaxed 测试 Case 6+7 通过
- [x] README「着色器翻译」小节描述单路径 `EShClientOpenGL` 管线 — grep `EShClientVulkan` 0 匹配，`EShClientOpenGL` 3 处
- [x] `Shader.cpp` 顶部管线注释已更新 — 9 步管线含两个归一化 pass
- [x] 引用 `EShClientVulkan` / 三级回退的行内注释已更新或移除 — grep `EShClientVulkan`/`vulkanRulesRelaxed` 0 匹配
- [ ] iOS arm64 dylib 经 ios-cmake 工具链构建成功 — **本 Linux 沙箱不可行（无 macOS/Xcode/MoltenVK.xcframework），须 CI/macOS 验证；原生 cmake 配置因子模块未初始化亦失败**
- [ ] `verify/syntax_check.sh` 通过 — **主机缺 `libvulkan-dev`/`glslang-dev`/`spirv-cross` 系统包，3 通过 / 26 缺头文件（非代码缺陷）；须装包后重跑或 CI 验证**
- [x] 更新后的 relaxed 测试在主机通过（若可构建） — **已构建并通过：7 用例 / 11 检查全过，exit=0（glslang 11.1.0-1501-g2eb8a581，ENABLE_OPT=OFF）**
- [ ] `nm -gU` 确认所构建 dylib 导出 `gl*` 与 `egl*` 符号 — **依赖 iOS dylib 产出，本环境不可行，须 CI/macOS 验证**
